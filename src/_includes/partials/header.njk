<header id="header" class="bg-white w-full px-6 py-5 z-50 fixed top-0 shadow-md transition-all transform ease-in-out duration-500">
    <div class="max-w-5xl mx-auto flex items-center flex-wrap justify-between">
        <div class="sm:mr-8 flex items-center space-x-3">
            <a href="{{ '/' | url }}" class="flex items-center space-x-3">
                <img src="/assets/img/Inside_the_Dugout_Logo.png" alt="Site Logo" class="h-16 w-auto">
                <span class="text-xl text-teal-700 font-semibold self-center">{{ site.title }}</span>
            </a>
        </div>
        <nav id="menu" class="order-last md:order-none items-center grow w-full md:w-auto md:flex hidden mt-2 md:mt-0 justify-evenly">
            {% for item in menu %}
            <a href="{{ item.url | url }}" {% if item.newTab %}target="_blank" rel="noopener" {% endif %} class="block mt-4 md:mt-0 font-medium text-slate-700 hover:text-teal-600 text-base">{{ item.label }}</a>
            {% endfor %}
        </nav>
        <form id="search" action="{{ '/search' | url }}" class="order-last sm:order-none grow items-center justify-end hidden sm:block mt-6 sm:mt-0">
            <label class="visually-hidden" for="header-searchbox">Hier suchen ...</label>
            <input type="text" id="header-searchbox" name="q" placeholder="Hier suchen ..." class="w-full sm:max-w-xs bg-slate-200 border border-transparent float-right focus:bg-white focus:border-slate-300 focus:outline-hidden h-8 p-4 placeholder-slate-500 rounded-sm text-slate-700 text-sm">
        </form>
        <div id="menu-toggle" class="flex items-center md:hidden text-slate-700 hover:text-teal-600 cursor-pointer sm:ml-6">
            {% include "icons/menu.svg" %}
        </div>
    </div>
</header>